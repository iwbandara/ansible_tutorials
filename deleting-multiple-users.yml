# Removing multiple users with the 'loop' command

    tasks:
      - name: Removing multiple users
        user:
          name: "{{ item }}"
          state: absent
          remove: true

        loop:
          - user1
          - user2
          - user3

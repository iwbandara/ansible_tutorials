# How to recursiively change the ownership of a directory

    - name: Recursively change ownership of a directory
      file:
        path: /tmp/foo
        state: directory
        recurse: yes
        owner: root
        group: root
